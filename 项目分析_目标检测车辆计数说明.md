# TrafficTracker 项目分析结论

> 分析日期：2026-08-07
> 分析对象：`F:\plate_project\0726\TrafficTracker`

## 一、结论

**该文件夹是一个基于 YOLOv8 的目标检测程序，专门用于「车辆与行人的检测 + 各类别数量统计」。** 三项判定要素全部成立：

| 判定项 | 是否成立 | 证据 |
| --- | --- | --- |
| 目标检测模型推理逻辑 | ✅ 成立 | `utils/detector.py` 的 `YOLODetector` 类，使用 `ultralytics.YOLO` 加载 `model/best.pt` 并执行推理 |
| 车辆识别与计数模块 | ✅ 成立 | `CLASS_MAPPING` 定义 6 个类别（5 类车辆 + 行人），`_count_objects()` 做按类别计数，UI 实时展示计数面板与 CSV 统计 |
| 相应数据处理流程 | ✅ 成立 | 图片/视频/批量图片三套处理链路、多线程视频推理、基于 IoU 的轨迹追踪、结果落盘（CSV/图/视频） |

---

## 二、关键源码与配置证据

### 1. 目标检测推理逻辑（`utils/detector.py`）
- 第 9 行：`from ultralytics import YOLO`，第 49 行：`self.model = YOLO(model_path)` 加载模型。
- 模型文件：`model/best.pt`（约 6.2 MB，Ultralytics YOLOv8 权重格式）。
- 推理入口：
  - `detect_image()`（第 69 行）`results = self.model(image_path, conf=, iou=)[0]`
  - `detect_video_frame()`（第 93 行）对视频帧推理。
- 边界框绘制 `_draw_boxes()`：用 OpenCV 画框、PIL 绘制中文类别标签（黑体/思源黑体等字体回退）。

### 2. 车辆识别与类别计数（`utils/detector.py` + `ui/main_window.py`）
- 类别映射（第 17–24 行）：`Pedestrian 行人`、`Van 面包车`、`Car 小汽车`、`Two-wheeler 两轮车`、`Bus 公交车`、`Truck 卡车` —— **6 类中 5 类属于车辆**，符合"检测车辆并统计"的定位。
- 计数方法 `_count_objects()`（第 103–123 行）：遍历 `results.boxes`，按类别名累加，返回 `{类别: 数量}` 字典。
- UI 统计面板（`main_window.py` 第 559 行起）：左侧面板以「行人/小汽车/两轮车/公交车/卡车」网格 + 进度条实时展示各类型数量；「目标数目」显示总目标数。
- 批量检测 CSV（`main_window.py` 第 1316–1371 行）输出列：`文件名,行人,小汽车,两轮车,公交车,卡车,总目标数`，对每张图按类别计数落盘。

### 3. 数据处理流程
- **图片**：`_on_select_image_clicked`（单选）/`_on_select_image_list_clicked`（文件夹批量遍历） → 推理 → 计数 → 画框 → 显示/落盘。
- **视频**：`VideoProcessThread`（继承自 `QThread`，第 24 行）逐帧推理；`_match_detections()`（第 63 行）基于 IoU 的贪心匹配做跨帧轨迹追踪（track_id），绘制运动轨迹线。
- **结果保存**：`results/YYYYMMDD_HHMMSS/` 目录下生成 `检测图像.png`、批量 `检测结果统计.csv`、可选 `detection_video.mp4`。
- **结果产物已验证**：`results/批量检测_*` 下 CSV 实际包含每行按类别计数（如 `0,1,0,0,1,2`），证明计数流程真实运行过。

### 4. 依赖配置
- `requirements.txt`：`numpy`、`opencv_python`、`Pillow`、`PySide6`(+Addons/Essentials) ，构成视觉处理与 GUI 基础。
- `运行教程.md`：要求额外安装 `torch`（GPU/CPU 版）、`ultralytics`（YOLOv8 官方库）、`QtFusion==0.5.4`，与源码直接对应。
- 本地 `QtFusion-0.5.4-py3-none-any.whl` 离线包佐证。

---

## 三、补充说明（不影响定性）

- **用户系统为辅助模块**：`database/database.py` + `ui/login.py`/`register.py`/`forgot_password.py` 提供登录/注册/找回密码（SQLite `user_data.db`），属于应用外壳，不参与检测核心逻辑。
- **辅助工具**：`utils/common.py` 提供 OpenCV/PIL → QPixmap 转换、耗时格式化、置信度配色。
- **程序入口**：`main.py`（`App` 栈式窗口，先登录再进主窗口），主窗口标题明确为"基于YOLOv8的车辆行人检测系统"。

---

## 四、最终判定

> **是。** TrafficTracker 是一个典型的桌面端（PySide6）YOLOv8 目标检测应用：加载 `best.pt` 权重对图片/视频/批量图片做推理，识别并逐类别统计车辆（小汽车、面包车、公交车、卡车、两轮车）与行人，实时显示计数并可将统计结果保存为 CSV/图片/视频。
